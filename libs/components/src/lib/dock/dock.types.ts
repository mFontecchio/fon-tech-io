/**
 * Dock component type definitions
 */

/**
 * Position of the dock relative to the viewport.
 * Determines which edge the dock attaches to and whether it uses
 * horizontal (top/bottom) or vertical (left/right) layout.
 */
export type DockPosition = 'top' | 'right' | 'bottom' | 'left';
